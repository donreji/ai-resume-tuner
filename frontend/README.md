# Frontend — AI Resume Tuner

React + Vite frontend for the AI Resume Tuner project.

See the [project README](../README.md) for full setup and usage instructions.

## Dev server

```bash
npm install
npm run dev
```

Runs on [http://localhost:5173](http://localhost:5173) and proxies `/api/*` to the backend at `http://localhost:3001`.
